# These values are public and bundled into browser JavaScript. Never add secrets here.
VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_API_PREFIX=/api/v1
# Production uses the page origin by default. Set this only when the production
# API is intentionally hosted on a different origin.
# VITE_PRODUCTION_API_BASE_URL=https://api.example.com
